import React from 'react';
import 'jest-dom/extend-expect';
import { render, cleanup } from '@testing-library/react'

afterEach(cleanup)

test('calls handle function once after click', () => {
    
})